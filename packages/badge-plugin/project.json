{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "badge-plugin",
  "root": "packages/badge-plugin",
  "sourceRoot": "packages/badge-plugin/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/badge-plugin",
        "main": "packages/badge-plugin/src/index.ts",
        "tsConfig": "packages/badge-plugin/tsconfig.json",
        "assets": [
          "packages/badge-plugin/README.md",
          "packages/badge-plugin/CHANGELOG.md",
          {
            "input": "packages/badge-plugin/src/ui",
            "glob": "**/*",
            "output": "./src/ui"
          },
          {
            "input": "packages/badge-plugin/src/dashboard",
            "glob": "**/*",
            "output": "./src/dashboard"
          }
        ]
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "config": "vitest.config.ts",
        "passWithNoTests": true,
        "coverage": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/badge-plugin/**/*.ts"]
      }
    },
    "upgrade:vendure": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm update @vendure/* --save",
        "cwd": "packages/badge-plugin"
      }
    },
    "version": {
      "executor": "@nx/workspace:version",
      "dependsOn": ["update-readme"],
      "options": {
        "changelog": true,
        "push": true
      }
    },
    "update-readme": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx scripts/update-readmes.ts badge-plugin"
      }
    }
  }
}
