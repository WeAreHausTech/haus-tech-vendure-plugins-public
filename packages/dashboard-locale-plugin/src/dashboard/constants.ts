/**
 * Mirrors `APPLIED_DEFAULTS_KEY` in `../constants.ts`. The Dashboard extension is compiled
 * separately from the server code and must not import it; a unit spec keeps the two equal.
 */
export const APPLIED_DEFAULTS_KEY = 'haus.dashboardLocale.appliedDefaults'
